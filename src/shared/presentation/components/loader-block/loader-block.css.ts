import { style } from "@vanilla-extract/css";

const wrapper = style({
  width: "100%",
  height: "100%",
  display: "flex",
  alignItems: "center",
  justifyContent: "center"
});

const classes = {
  wrapper
};

export default classes;
